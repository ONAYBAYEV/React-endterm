import React from 'react'
import logo from '../img/bg_logo.jpeg'

const Header = () => {
    return (
        <header className="center">
            <img src={logo} />
        </header>
    )
}

export default Header
